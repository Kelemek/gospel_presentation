{
  "name": "gospel-presentation",
  "version": "1.0.0",
  "description": "Gospel presentation website with ESV API integration",
  "scripts": {
    "build": "node build-config.js",
    "build-config": "node build-config.js",
    "dev": "npm run build-config && python3 -m http.server 8000",
    "start": "npm run build-config && python3 -m http.server 8000",
    "netlify-build": "node build-config.js",
    "preview": "npm run build && python3 -m http.server 8000"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  },
  "keywords": ["gospel", "esv", "api", "scripture"],
  "author": "Dr. Stuart Scott",
  "license": "MIT"
}